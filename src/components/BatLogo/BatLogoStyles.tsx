import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    justifyContent: 'center',
    fontSize: 30,
    fontWeight: 'bold',
    color: '#E5BF3C',
  },
  batImage: {
    resizeMode: 'contain',
    height: 180,
  }
});